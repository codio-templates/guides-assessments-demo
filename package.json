{
  "name": "workspace",
  "version": "0.0.0",
  "description": "example codio app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "cheerio": "~0.20.0"
  }
}
